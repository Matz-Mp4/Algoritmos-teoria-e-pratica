/* Enunciado: Crie um algoritmo de tempo O(n * lg(n)) que, dado, um conjunto S
 * de n inteiros e um outro inteiro x, determine se existem ou não dois elentos em
 * S cuja soma seja extamente x  */

#include <stdbool.h>
#include <stdio.h>

bool buscarSoma(int array[], int n){
  bool achou;

  return achou;
}
int  main() {

  int array[10] = {20, 3, 5, 1, 4, 10, 39, 3, 10, 3};

  printf("\nLista: ");
  for (int i = 0; i < 10; i++) {
    printf("%d ", array[i]);
  }

  return 0;
}
